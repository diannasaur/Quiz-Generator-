# Quiz-Generator
This program implements an Avalonia GUI application that loads 20 multiple-choice questions from a text file, displays them one at a time, lets the user select answers, and calculates the final score at the end.
